\begin{frame}{Agentic RAG với LangGraph 0}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth,height=0.74\textheight,keepaspectratio]{pictures/LangGraph/LangGraph0.png}
\caption{Sơ đồ LangGraph của dự án}
\end{figure}

\note{

Ở chế độ fast RAG chỉ dùng phân loại để giảm truy xuất dữ liệu và thực hiện trả lời với dữ liệu đã truy xuất phù hợp với kiến trúc RAG cơ bản
Ở chế độ voice do đã có llm ở sử dụng tool calling, lấy thông tin nhân vật và agentic law....
Người dùng không nhập đường dẫn url bằng giọng nói nên bỏ qua phân lọi và linker thực hiện trichd xuất số hiệu định danh văn bản

% Đây là sơ đồ Agentic RAG của dự án được xây dựng bằng LangGraph.

% Hệ thống phân chia thông minh giữa mô hình nhỏ cho

% cơ bản (màu xanh lá) và mô hình lớn cho các tác vụ suy luận phức tạp (màu xanh dương) để tối ưu chi phí và độ trễ.

% Mỗi tin nhắn đi qua Router để kiểm tra điều kiện và định tuyến qua các node như rewrite query, retrieve tài liệu và sinh câu trả lời.

}
\end{frame}
