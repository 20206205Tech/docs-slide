% Các bước trong đường ống dữ liệu VBPL cũ
\begin{frame}{Các bước trong đường ống dữ liệu VBPL}
\begin{figure}[H]
\centering
\includegraphics[width=\linewidth,height=0.74\textheight,keepaspectratio]{pictures/step0.excalidraw.png}
\caption{Sơ đồ các bước thực hiện của đường ống dữ liệu VBPL cũ}
\end{figure}

\note{

Đây là Sơ đồ các bước thực hiện của đường ống dữ liệu VBPL cũ

\hrule

Vì số lượng VBPL lớn nên cần chia nhỏ công việc

Lấy tổng + lấy danh sách + lấy chi tiết các VBPL

\hrule

Tiếp theo:
Trích xuất thông tin + nội dung HTML + chuyển đổi nội dung HTML thành định dạng MARKDOWN

\hrule

Cuối cùng thực hiện quá trình RAG:

Tóm tắt + chucking bằng LLM + tạo ngữ cảnh + vector hóa và lưu CSDL vector

(pinecone)

}
\end{frame}

% đây là sơ đồ các bước thực hiện của đường ống dữ liệu văn bản pháp luật cũ

% vì số lượng văn bản pháp luật lớn nên cần chia nhỏ công việc, lấy tổng, lấy danh sách, lấy chi tiết các văn bản pháp luật

% tiếp theo, trích xuất thông tin, nội dung html, chuyển đổi nội dung html thành định dạng mắc đao

% cuối cùng thực hiện quá trình rag: tóm tắt, chăng king bằng llm, tạo ngữ cảnh, vector hóa và lưu cơ sở dữ liệu vector pai côn
